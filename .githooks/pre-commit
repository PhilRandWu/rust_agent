#!/bin/sh
set -e

cd "$(git rev-parse --show-toplevel)/backend"

echo "==> cargo fmt"
cargo fmt --all -- --check

echo "==> cargo test"
cargo test

echo "==> cargo check"
cargo check

echo "==> git diff --check"
git diff --check
